/**
 * Este é um arquivo de configuração que informa ao SEQUELIZE o caminho
 * onde está localizado os parametros de configuração e acesso ao DB.
 */
const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js')
    /*,
        'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),*/
};